#!/bin/sh -x
firefox "$@"
exit

echo "$@" >/tmp/txt

if echo $2 | grep openURL; then
    cmd=`echo $2 |sed -e 's/,new-window)$/,new-page)/'`
    echo "$cmd" >>/tmp/osd/default
    /usr/bin/opera -remote "$cmd"
    exit
fi

echo "$@" >>/tmp/osd/default
/usr/bin/opera -newpage "$@"
