#!/usr/bin/perl

exit unless @ARGV;
$share=shift @ARGV;
$user=shift @ARGV || "dgrana";

$mountpoint="~/mnt/$share";
`mkdir -p $mountpoint` unless -d $mountpoint;
system("smbmount $share $mountpoint -o username=$user,users,iocharset=iso8859-15");

